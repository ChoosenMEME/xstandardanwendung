services:
  web:
    container_name: xstandardanwendung-dev
    # Lokaler Build aus dem Dockerfile statt Pull vom Docker Hub.
    build:
      context: .
    image: choosenmeme/xstandardanwendung:dev
    restart: unless-stopped
    ports:
      - "${WEB_PORT:-8000}:${WEB_PORT:-8000}"
    environment:
      DEBUG: ${DEBUG:-1}
      SECRET_KEY: ${SECRET_KEY:-dev-secret-key}
      DJANGO_ALLOWED_HOSTS: ${DJANGO_ALLOWED_HOSTS:-localhost 127.0.0.1 [::1]}
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
      WEB_HOST: ${WEB_HOST:-0.0.0.0}
      WEB_PORT: ${WEB_PORT:-8000}
      APP_PATH: ${APP_PATH:-}
      LANGUAGE_CODE: ${LANGUAGE_CODE:-de-de}
      TZ: ${TZ:-UTC}
    volumes:
      # Quellcode live ins Container-/app spiegeln (inkl. db.sqlite3 + Tests).
      - ./app:/app
